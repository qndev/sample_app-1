<%= link_to gravatar_for(current_user, size: Settings.micropost.avatar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("static_pages.users.link_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
